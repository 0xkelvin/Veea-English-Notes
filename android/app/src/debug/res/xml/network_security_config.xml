<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only. Android has blocked cleartext HTTP since API 28, which would stop
  a debug build reaching a backend running on the development machine.

  This file lives in src/debug, so it is never merged into a release build —
  release traffic still requires HTTPS.

  10.0.2.2 is how the Android emulator reaches the host machine's localhost.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
